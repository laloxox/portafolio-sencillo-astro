---
import Section from "../Section.astro"


import { education } from "@cv"
---

<Section title="# Education">

        <ul>
            {
            education.map(
                ({ institution, area, url, startDate, endDate}) => {
                return (
                <li>
                    <h3>{institution}</h3>
                    <h4>{area}</h4>
                    <a>{url}</a>
                    <p>Start Date:{startDate}</p>
                    <p>{endDate}</p>
                    
                </li>
                )
            }
        )
    }

</Section>


<style>
    h4 {
        margin-top: 3px;
        margin-bottom: 8px;
    }

    p {
        margin-top: 3px;

    }
    
    


</style>